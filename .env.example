# ================================================
# Hawks Coach Hub - Environment Configuration
# ================================================

# Google Service Account Configuration
# Choose ONE of the following authentication methods:

# Option 1: Direct JSON key content (RECOMMENDED for production/Vercel)
# Copy the entire service account JSON content as a single line
GOOGLE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"your-project-id","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"hawks-drive-reader@your-project.iam.gserviceaccount.com","client_id":"...","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs","client_x509_cert_url":"..."}

# Option 2: Path to JSON key file (for local development only)
# Place your service account key file in the project root (ignored by git)
GOOGLE_SERVICE_ACCOUNT_KEY_FILE="./coach-hub-469817-02211c9252a2.json"

# Hawks Google Drive Configuration
# Default Hawks training materials folder (public read access)
HAWKS_FOLDER_ID="1ZF6AHx62MXfkgs7-xbrMxj4r9sdyKzUb"

# Development Environment Settings
# Environment mode (development, production, test)
NODE_ENV="development"

# Port for local development server (default: 8000)
# PORT="8000"

# ================================================
# Security Notes:
# ================================================
# 1. NEVER commit the actual .env file to version control
# 2. Service account keys are automatically ignored by .gitignore
# 3. For production deployment, use environment variables in your hosting platform
# 4. Rotate service account keys quarterly for security
# 5. Grant minimal permissions (Google Drive API read-only)

# ================================================
# Google Cloud Service Account Setup:
# ================================================
# 1. Enable Google Drive API in Google Cloud Console
# 2. Create service account: "hawks-drive-reader"
# 3. Generate and download JSON key file
# 4. Share Hawks Drive folder with service account email
# 5. Set permissions to "Viewer" (read-only)
#
# See GITHUB-ACTIONS-SETUP.md for detailed setup instructions

# ================================================
# Local Development Quick Start:
# ================================================
# 1. Copy this file: cp .env.example .env
# 2. Add your service account key (Option 1 or Option 2)
# 3. Verify setup: npm run dev
# 4. Open http://localhost:8000
#
# Troubleshooting: See HAWKS-DEVELOPMENT-GUIDE.md