# Google Drive API Configuration
# Copy this file to .env and fill in your actual credentials
# NEVER commit .env to version control

GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_API_KEY=your-api-key-here

# Security Notes:
# 1. Restrict API key to specific domains in Google Cloud Console
# 2. Configure OAuth consent screen properly
# 3. Add only necessary authorized JavaScript origins
# 4. Use the most restrictive scope possible (drive.metadata.readonly)